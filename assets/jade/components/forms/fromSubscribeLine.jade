.container
    .subscribe-wrap.center
        .subscribe__header
            h2.subscribe__ttl Подписывайтесь
            p.subscribe__sub-ttl на рассылку новинок, акций и скидок
        form#subscribeLine.subscribe.subscribe--line(action='#', method='POST')
            .form-group
                input.subscribe__input.form-control(type='email', name='email', placeholder='Ваш e-mail*:', required)
            button.btn(type='submit', name='subscribe') Подписаться
